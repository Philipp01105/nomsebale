init

voll backup

id/hash = aus Metadaten (parent node + tree state hash + commit metadata

timestamp

parent node

1st commit

differenziell

id/hash = aus metadaten

timestamp

parent node											nach x commits oder zeit full snapshot + interner neuer tree
